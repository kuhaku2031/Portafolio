---
import { experiences } from "../scripts/tecnologies";
---

<div class="flex flex-col w-full my-6 gap-4">
  <h1 class="text-white text-2xl font-bold">Tecnologías</h1>

  <div class="flex flex-row flex-wrap gap-4">
    {
      experiences.map((experiences) => (
        <div class="flex px-5 py-2 bg-theme-dark-purple rounded-3xl gap-1">
          {experiences.icon && (
            <div class="size-6 text-theme-malva">
              <experiences.icon class="" />
            </div>
          )}
          <p class=" text-theme-malva font-normal">{experiences.tecnologie}</p>
        </div>
      ))
    }
  </div>
</div>
